@page "/AccessDenied"
@page "/AccessDenied/{Message}"

<HackSystemMessage Message="@message"></HackSystemMessage>

@code{
    private string message = "Access Denied";

    [Parameter]
    public string Message { get => message; set => message = value; }
}